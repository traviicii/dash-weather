# Weather Dashboard

A visually rich, single‑page weather dashboard built with React + Vite + TypeScript.

## Quick start

```bash
npm install
npm run dev
```

## Notes

- Uses Open‑Meteo geocoding + forecast endpoints (no API key required).
- Default units are imperial with a toggle to metric.
- Geolocation is requested on first load and falls back to quick city buttons.
